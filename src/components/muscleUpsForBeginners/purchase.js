import { Button, Col } from "antd";

export const Purchase = () => {
  return (
    <Col span={24} style={{ textAlign: "center" }}>
      <Button style={{ background: "black", color: "white", width: "50%" }}>
        Purchase
      </Button>
    </Col>
  );
};
